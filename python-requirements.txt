# python libraries required for neovim
# host-only packages go here; CLI tools (ipython, pyright, ruff, uv) come from pipx
# project dependencies are managed per project with `uv venv .venv` + `uv pip install ...`
# install this file with: python3 -m pip install --user -r python-requirements.txt

# neovim python provider - required for python integration
pynvim>=0.5.0
